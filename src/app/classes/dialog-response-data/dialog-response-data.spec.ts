import { DialogResponseData } from './dialog-response-data';

describe('DialogResponseData', () => {
  it('should create an instance', () => {
    expect(new DialogResponseData()).toBeTruthy();
  });
});
